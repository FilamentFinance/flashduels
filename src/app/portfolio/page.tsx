'use client';

import PortfolioComponent from '@/components/portfolio';
import { FC } from 'react';

const Portfolio: FC = () => {
  return (
    <main>
      <PortfolioComponent />
    </main>
  );
};

export default Portfolio;
